package ru.fastdelivery.domain.common.dimension;

import java.math.BigInteger;

public class Length extends Size {
    public Length(BigInteger lengthMillimeters) {
        super(lengthMillimeters);
    }
}
